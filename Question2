@app.route('/account/<user_id>')
def get_account(user_id, password):
    user = db.query(User).filter_by(id=user_id).first()
	user_password = user.get_password(user)
	if (user_password != password) {
		return null
	} 
	else {
		return jsonify(user.to_dict())
	}
